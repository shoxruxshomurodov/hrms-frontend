import React from 'react';
import ListContainer from "../containers/ListContainer";

const ListPage = ({
                      ...rest
                  }) => {
    return (
        <>
            <ListContainer />
        </>
    );
};

export default ListPage;
