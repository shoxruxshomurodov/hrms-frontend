import React from 'react';
import DocumentCreateContainer from "../containers/DocumentCreateContainer";

const DocumentCreatePage = (props) => {
    return (
      <DocumentCreateContainer />

    );
};

export default DocumentCreatePage;
