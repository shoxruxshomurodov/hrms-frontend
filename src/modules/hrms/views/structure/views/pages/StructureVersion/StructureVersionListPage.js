import React from 'react';
import StructureVersionListContainer from "../../../containers/StructureVersion/StructureVersionListContainer";

const StructureVersionListPage = ({...rest}) => {
    return (
        <>
            <StructureVersionListContainer {...rest} />
        </>
    );
};

export default StructureVersionListPage;
