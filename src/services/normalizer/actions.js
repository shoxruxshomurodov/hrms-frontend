import { createRoutine } from "redux-saga-routines";
const NORMALIZE = createRoutine("NORMALIZE");
const CHANGE_NORMALIZE = createRoutine("CHANGE_NORMALIZE");
export default {
    NORMALIZE,
    CHANGE_NORMALIZE
};
